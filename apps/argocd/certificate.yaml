apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-tls
  namespace: argocd
spec:
  secretName: argocd-server-tls
  commonName: argocd.home.arpa
  dnsNames:
    - argocd.home.arpa
  duration: 168h
  renewBefore: 24h
  issuerRef:
    kind: ClusterIssuer
    name: home-ca-issuer
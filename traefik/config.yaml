---
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    ports:
      immich:
        port: 8812
        expose: true
        exposedPort: 8812
        protocol: TCP